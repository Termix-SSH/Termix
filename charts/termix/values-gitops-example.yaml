image:
  repository: ghcr.io/lukegus/termix
  tag: latest

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: termix.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: termix-tls
      hosts:
        - termix.example.com

persistence:
  enabled: true
  size: 20Gi

secrets:
  create: false

database:
  dialect: sqlite

extraEnvFrom: []
