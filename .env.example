# ============================================================================
# Termix Environment Configuration Example
# ============================================================================
# Copy this file to .env and configure your settings
# cp .env.example .env

# ----------------------------------------------------------------------------
# Reverse Proxy Base Path
# ----------------------------------------------------------------------------
# Set this when deploying Termix behind a reverse proxy under a subpath
#
# Examples:
#   - Root deployment:    Leave empty or set to /
#   - Subpath deployment: VITE_BASE_PATH=/termix
#
# This affects:
#   - Static asset URLs (JS, CSS, images)
#   - API request paths
#
# Nginx reverse proxy example:
#   location /termix/ {
#       proxy_pass http://localhost:30001/;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   }
#
# VITE_BASE_PATH=/termix

# ----------------------------------------------------------------------------
# HTTPS Configuration
# ----------------------------------------------------------------------------
# Enable HTTPS for development server
# Requires SSL certificates in ssl/termix.crt and ssl/termix.key
# VITE_HTTPS=true

# ----------------------------------------------------------------------------
# API Host (Development Only)
# ----------------------------------------------------------------------------
# Override API host for development
# Default: localhost
# VITE_API_HOST=localhost
