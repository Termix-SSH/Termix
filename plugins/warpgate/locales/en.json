{
  "settings": {
    "host": {
      "useWarpgate": {
        "label": "Warpgate Gateway",
        "description": "This host connects through a Warpgate SSH proxy. Termix will handle the browser-based approval flow automatically after authenticating."
      }
    }
  }
}
